#include <stdio.h>
#include <wiringPi.h>

int main(){
	const int led_pin = 1;
	wiringPiSetup();
	pinMode(led_pin,OUTPUT);

	while(1){
		int flag = 0;
		scanf("%d",&flag);
		digitalWrite(led_pin,flag);
	}	
}