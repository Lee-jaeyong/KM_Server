package ljy.book.admin.jpaAPI;

import ljy.book.admin.common.repository.CommonRepository;
import ljy.book.admin.entity.KM_subject;

public interface KM_subjectAPI extends CommonRepository<KM_subject, Long> {
	//KM_subject findBySeq(Long seq);
}
